<VirtualHost *:8080>
    DocumentRoot "/app/public"
    ServerName localhost

    RewriteRule ^(/conf|/src$) - [F]
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_FILENAME} !-d
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [NS,PT]
</VirtualHost>